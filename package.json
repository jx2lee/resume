{
  "private": true,
  "type": "module",
  "scripts": {
    "kr": "resumed --theme jsonresume-theme-paper resume-kr.json --output resume-kr.html && node index.js resume-kr.json",
    "en": "resumed --theme jsonresume-theme-paper resume-en.json --output resume-en.html && node index.js resume-en.json"
  },
  "dependencies": {
    "jsonresume-theme-paper": "^0.5.0",
    "resumed": "^3.0.1",
    "puppeteer": "^13.0.0"
  }
}
